<div class="profile">

<div class="row">
  <div class="small-3 small-centered columns" id="avatar">
    <%= image_tag(@user.avatar_url) if @user.avatar? %>
  </div>
</div>
<div class="row avatar-update">
  <div class="small-3 small-centered columns">
    <% if @valid_user %>
      <%= link_to "Edit Acct Details", edit_user_registration_path %>
    <% end %>
  </div>
</div>

<%= react_component("UserData", props: { user: @user,
                                  instruments: @user_instruments,
                                  genres: @user_genres,
                                  gigs: @user_gigs,
                                  gigRequests: @user_gig_requests,
                                  bandRequests: @user_band_requests,
                                  genresAll: @all_genres,
                                  instrumentsAll: @all_instruments,
                                  validUser: @valid_user}) %>

<%= react_component("SpotifyContainer", props: {user: @user,
                                                tracks: @user_tracks,
                                                validUser: @valid_user}) %>

</div>
